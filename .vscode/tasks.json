{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "atomic: build bot",
      "type": "shell",
      "command": "yarn tsc -p tsconfig.json",
      "problemMatcher": []
    },
    {
      "label": "atomic: run compiled",
      "type": "shell",
      "command": "yarn node ./dist/src/index",
      "dependsOn": ["atomic: build bot"]
    },
    {
      "label": "atomic: build",
      "group": "build",
      "dependsOn": ["atomic: build bot", "atomic: run compiled"], 
      "problemMatcher": []
    }
  ]
}
